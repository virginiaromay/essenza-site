const btn=document.querySelector('.menu-toggle');const menu=document.getElementById('menu');btn?.addEventListener('click',()=>{const e='true'===btn.getAttribute('aria-expanded');btn.setAttribute('aria-expanded',String(!e)),menu.classList.toggle('show')});document.getElementById('year').textContent=(new Date).getFullYear();document.querySelectorAll('a[href^="#"]').forEach(a=>{a.addEventListener('click',e=>{const id=a.getAttribute('href').slice(1),el=document.getElementById(id);el&&(e.preventDefault(),el.scrollIntoView({behavior:'smooth',block:'start'}),menu?.classList.remove('show'))})});const chips=document.querySelectorAll('.chip');const tiles=document.querySelectorAll('.tile');chips.forEach(chip=>{chip.addEventListener('click',()=>{chips.forEach(c=>c.classList.remove('active'));chip.classList.add('active');const filter=chip.dataset.filter;tiles.forEach(t=>{const cat=t.dataset.category;const show='all'===filter||cat===filter;t.hidden=!show})})});